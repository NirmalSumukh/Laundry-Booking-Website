// frontend/public/scripts/main.js
// General page scripts: simple navbar interactions, helpers

document.addEventListener('DOMContentLoaded', () => {
  // Example animation
  const hero = document.querySelector('.hero-title');
  if (hero) {
    hero.classList.add('fade-in');
  }
});
